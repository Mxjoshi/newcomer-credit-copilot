# Phase 4 — Evaluate & Iterate (ship MVP)  ⬜ NOT STARTED

**Brief deliverable:** *"An MVP, an evals benchmark, and a first set of metrics measured against the
baseline."*

## Tasks
1. **Surface & validate risks** — list product risks, then the fastest/cheapest way to test each.
2. **Define & build the MVP** — scope the smallest product that tests the key risks (STAR format),
   then build it.
3. **Set up evals & measure** — build the evals benchmark, run v1 on **~20 diverse synthetic
   applicant profiles**, manually verify each output, compute the metrics, and compare to the
   Phase 1 targets.

## The metrics to measure (defined in Phase 1, measured here)
- Group A (credit): decision accuracy ≥80%, false-approval <10%, refer rate vs review capacity.
- Group B (AI output): hallucination <2%, policy grounding 100%, latency <15s.
- See `../01-scope-and-research/deliverables/03-success-metrics.md`.

## Also lives here
- **Presentation / pitch** — the final demo script and slides (the capstone is "ship + present").
- **Pre-presentation fact TODO:** if the deck cites the AECB 3.71M salary figure or the CBUAE
  minimum-salary removal, primary-source them (both currently rest on solid secondary reporting).

> Blocked until Phase 3 build is done.
